{
  "name": "danielwjchen.com-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end + visual-regression tests for danielwjchen.com. Compares the local dev site (npm run develop) against production (https://danielwjchen.com).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:update": "playwright test --update-snapshots",
    "baseline:prod": "BASE_URL=https://danielwjchen.com playwright test --update-snapshots",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
